# GodotRealityKit dependency configuration.
# Edit these values, or override via environment variables of the same name.
#
# By default, scons will clone and build godot/godot-cpp from these forks.
# To reuse a pre-built workspace instead (e.g. in CI), set the SHARED_WORKSPACE
# env var (or uncomment a SHARED_WORKSPACE= line here) before running scons.
GODOT_URL=https://github.com/rsanchezsaez/godot.git
GODOT_BRANCH=b2cd2726f65598d9dfdfc11242a2508551d5a629
GODOT_CPP_URL=https://github.com/godotengine/godot-cpp.git
GODOT_CPP_BRANCH=6c9ee5d7ea2d94051968c4c048309eb6b3b04b5d
